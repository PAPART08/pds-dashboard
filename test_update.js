const { updateTeamMember } = require('./src/app/actions/user.js'); // Actually, it's TS, so I'll write a standalone script calling the DB.

// wait, I can just use ts-node or since it's Next.js I'll write a simple test script.
